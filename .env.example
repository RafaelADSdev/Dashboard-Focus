# Copie para .env.local e preencha os valores.
# Nunca commite .env.local.

BITRIX_WEBHOOK_URL=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_ADMIN_EMAILS=
# Chave secreta do Supabase (Settings → API → service_role). Apenas no servidor.
SUPABASE_SERVICE_ROLE_KEY=
